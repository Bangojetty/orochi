{
  "$schema": "../gen/schemas/desktop-schema.json",
  "identifier": "default",
  "description": "Capabilities for the Orochi main and region-overlay windows",
  "windows": ["main", "overlay", "overlay-*"],
  "permissions": [
    "core:default",
    "dialog:default",
    "global-shortcut:default"
  ]
}
