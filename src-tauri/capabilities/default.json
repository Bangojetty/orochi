{
  "$schema": "../gen/schemas/desktop-schema.json",
  "identifier": "default",
  "description": "Capabilities for the Orochi main and overlay windows",
  "windows": ["main", "overlay"],
  "permissions": [
    "core:default",
    "dialog:default",
    "global-shortcut:default"
  ]
}
