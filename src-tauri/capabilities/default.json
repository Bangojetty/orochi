{
  "$schema": "../gen/schemas/desktop-schema.json",
  "identifier": "default",
  "description": "Capabilities for the Orochi main, overlay and cursor windows",
  "windows": ["main", "overlay", "overlay-*", "cursor"],
  "permissions": [
    "core:default",
    "dialog:default",
    "global-shortcut:default"
  ]
}
